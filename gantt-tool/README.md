# gantt-tool

一个零依赖（纯前端）的本地小工具，用于多项目月度计划录入、文本抽取、自动计算和每项目甘特图展示。

## 已实现功能

1. 多项目管理：每个项目独立数据、独立甘特图。
2. 项目基础信息：覆盖任务名称、部门、责任人、立项/验收日期等字段。
3. 文本抽取：从输入文本中提取常见字段（可人工校正）。
4. 月度明细表：支持新增/编辑/删除任务明细。
5. 自动计算：
   - 周期（天）= 预计完成日期 - 预计开始日期 + 1
   - 任务有实际完成日期时自动将进度回填为 100（可再改）
   - 整体执行百分比按周期加权计算
6. 甘特图：按当前项目渲染，支持按月份过滤，支持延期高亮、实际完成标记。
7. 数据持久化：浏览器本地存储（localStorage）。
8. 数据导入导出：JSON 文件。

## 使用方式

1. 直接用浏览器打开 `index.html`。
2. 左侧新建项目，填写项目基础信息。
3. 在“文本抽取”区域粘贴描述，点击“从文本提取”。
4. 在“工作计划明细”中维护每月任务。
5. 在“项目甘特图”查看当前项目进度。

## 后续建议（进入工程化阶段）

1. 接入后端数据库（SQLite/PostgreSQL）替代 localStorage。
2. 增加登录和权限（按部门/角色）。
3. 引入更强 NLP/LLM 抽取能力，并保留人工确认流程。
4. 增加跨项目汇总看板和导出报表（Excel/PDF）。
